/* Linters are special tools for spelling code rules */

let age = 18;
let condition = age >= 18 ? true : false;
if (condition) {
    console.log("That's ok. You can enter the pub.");
} else {
    console.log("Sorry, but you are a minor.");
}